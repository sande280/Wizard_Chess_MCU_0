// main entrypoint wrapper disabled — real sources live under `src/`.
// Keeping this file as a harmless placeholder so the `main/` folder
// doesn't accidentally register a separate component.

// (no-op)
