## main component intentionally disabled.
## This CMakeLists does not register any IDF component so the build
## system will not create a separate 'main' target. The project uses
## the `src/` layout (the __idf_src target) instead.

# Note: leave this file present but inert to avoid accidental re-activation.
